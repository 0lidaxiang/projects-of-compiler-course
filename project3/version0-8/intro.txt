

1.example.go已经通过,只有负号好像没有输出。但jasm代码与老师给的文件里一样的，有ineg这一行。所以结果为什么没有，未知。

2.fib.go只有const有问题了，其他没问题。
const
这里删除了上三行的多余，但这样的话，lbody只能执行一次，所以其实const也不行。只能报错。除非自己新增一个全局变量，但那样就改变了const的意义。
所以最后这一版的办法是，把这里的范例fib.go注释了修改const变量的那一行。

3.HelloWorld.go已经通过，但需要加上void，原来的是错误的写法。
